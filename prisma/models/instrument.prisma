model Instrument {
    // ID
    id String @id @default(uuid())

    // Required Attributes
    name String @unique
    slug String @unique

    // Optional Attributes
    description String?

    // Metadata
    Profiles      Profile[]
    PracticeParts PracticePart[]
}
