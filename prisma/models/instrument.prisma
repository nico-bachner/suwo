model Instrument {
    // ID
    id String @id @default(uuid())

    // Required Attributes
    name String @unique

    // Optional Attributes
    description String?

    // Metadata
    PracticeParts  PracticePart[]
    UserInstrument UserInstrument[]
}
