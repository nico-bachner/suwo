model Instrument {
    // Required Attributes
    name String @unique
    slug String @unique

    // Optional Attributes
    description String?

    // Metadata
    Profiles      Profile[]
    PracticeParts PracticePart[]

    @@index([name(sort: Asc)])
}
