model Event {
    // ID
    id String @id @default(uuid())

    // Required Attributes
    name      String
    starts_at DateTime

    // Optional Attributes
    ends_at  DateTime?
    location String?
    notes    String?
    type     String?

    // Metadata
    created_at DateTime @default(now())
    updated_at DateTime @default(now()) @updatedAt
}
