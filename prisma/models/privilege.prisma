model Privilege {
    // ID
    id String @id @default(uuid())

    // Required Attributes
    name String @unique

    // Relations
    grantees Role[]

    // Metadata
    created_at DateTime @default(now())
    updated_at DateTime @default(now()) @updatedAt
}
